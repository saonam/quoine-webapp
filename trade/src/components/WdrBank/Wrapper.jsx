import React from 'react';

import TogglerWrapper from '@quoine/components/Toggler/Wrapper';

import View from './View';

const WdrBank = (props) => (
  <TogglerWrapper Element={View} {...props} />
);

export default WdrBank;
