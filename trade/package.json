{
  "name": "@quoine/trade",
  "version": "3.10.0-rc.6",
  "dependencies": {
    "@quoine/core": "file:./../core",
    "@quoine/react-scripts": "5.9.13",

    "react": "^15.5.0",
    "react-dom": "^15.5.0",
    "react-redux": "^5.0.1",
    "react-router": "^3.0.0",
    "redux": "^3.6.0",
    "redux-saga": "^0.14.0",
    "reselect": "^2.5.4",
    "prop-types": "^15.5.8",

    "cryptowatch-embed": "^0.3.1",
    "lodash.throttle": "^4.1.1",
    "qrcode.react": "^0.6.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",

    "postinstall": "if [ -z ${DYNO+x} ]; then cd ../core && yarn install && yarn link && yarn link @quoine/core && cd ../trade && yarn link @quoine/core; fi"
  }
}
